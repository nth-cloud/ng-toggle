
name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

defaults:
  run:
    shell: bash

jobs:
  tests:
    name: Unit Tests
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node: [12, 14, 16]

    steps:
      - uses: actions/checkout@v2

      - name: Setup Chrome
        uses: browser-actions/setup-chrome@latest
        with:
          chrome-version: stable

      - name: Setup Node.js environment
        uses: actions/setup-node@v2.4.0
        with:
          node-version: "${{ matrix.node }}"

      - name: Setup Yarn
        uses: sergioramos/yarn-actions@v6
        with:
          no-lockfile: true
          version: true

      - name: Install Dependencies
        run: yarn

      - name: Saucelabs Connect
        uses: saucelabs/sauce-connect-action@v1
        with:
          username: ${{ secrets.SAUCE_USERNAME }}
          accessKey: ${{ secrets.SAUCE_ACCESS_KEY }}
          tunnelIdentifier: github-action-tunnel
          scVersion: 4.6.4

      - name: Run Tests
        run: |
          yarn ci
          yarn api-doc:test

#      - name: Run Browser Tests
#        env:
#          TRAVIS_BUILD_NUMBER: "${{ env.GITHUB_RUN_NUMBER }}"
#          TRAVIS_BUILD_ID: "${{ env.GITHUB_RUN_ID }}"
#          TRAVIS_JOB_NUMBER: github-action-tunnel
#        run: |
#          yarn saucelabs
#          yarn saucelabs:ie
